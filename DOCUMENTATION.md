# 🐱 Weather & Planes Monitor (Yar-Sale)

Документация к проекту интерактивного монитора погоды, авиарейсов и состояния зимников для Яр-Сале (ЯНАО).

## 📋 О проекте

Веб-приложение отображает полезную информацию для жителей Ямала в развлекательной форме:
*   **Погода:** Текущая температура, ощущаемая температура, ветер (анимированный пропеллер у мыши), осадки (снег/дождь).
*   **Авиарейсы:** Табло прилета/вылета аэропорта Салехард.
*   **Зимники:** Статус и тоннаж зимних автодорог (Яр-Сале, Аксарка, Салемал и др.).
*   **Визуал:** Новогодняя тематика 2026, анимированный вертолет, самолеты, кот в шапке Санты.

---

## 🚀 Быстрый старт (Локально)

Для запуска и разработки на локальном компьютере:

1.  **Требования:**
    *   Node.js (v18 или выше).
    *   Браузер (Chrome/Firefox).

2.  **Установка зависимостей:**
    ```bash
    npm install
    # Установка браузеров для Playwright (нужно для скрейперов)
    npx playwright install
    ```

3.  **Запуск сбора данных (вручную):**
    Скрипты создадут файлы `flights.json` и `zimnik.json`.
    ```bash
    # Обновить расписание рейсов
    node scripts/scrape.js

    # Обновить статус зимников
    node scripts/scrape_zimnik.js
    ```

4.  **Запуск сайта:**
    Просто откройте `index.html` в любом браузере.
    *Рекомендуется использовать Live Server в VS Code для корректной работы путей.*

---

## 🏗 Архитектура

Проект состоит из двух независимых частей: **Frontend** (визуализация) и **Backend/Automation** (сбор данных).

### 1. Frontend (Сайт)
*   **Технологии:** HTML5, CSS3, Vanilla JS (без фреймворков).
*   **Точка входа:** `index.html`.
*   **Логика:** При загрузке страницы `js/main.js` инициализирует модули. JS считывает заранее подготовленные JSON-файлы (`flights.json`, `zimnik.json`) и запрашивает погоду у внешнего API.

### 2. Backend (Сбор данных)
Данные собираются скриптами на Node.js (папка `scripts/`) и сохраняются в статические JSON-файлы.
*   **Инструмент:** [Playwright](https://playwright.dev/) (эмуляция браузера для парсинга сайтов).
*   **Автоматизация:** GitHub Actions запускает скрипты по расписанию и делает коммит с обновленными файлами обратно в репозиторий.

---

## 📡 Источники данных

| Данные | Источник | Скрипт / API | Обновление | Примечание |
| :--- | :--- | :--- | :--- | :--- |
| **Погода** | Open-Meteo API | `js/weather.js` | В реальном времени | Запрос с клиента (браузера). Координаты в `js/config.js`. |
| **Рейсы** | slyport.ru | `scripts/scrape.js` | Раз в 20 минут | Парсит табло вылета и прилета. Учитывает защиту DDoS-Guard. |
| **Зимники** | map.yanao.ru | `scripts/scrape_zimnik.js` | Раз в 6 часов | Парсит интерактивную карту. **Требует прокси!** |

---

## 🤖 Автоматизация (GitHub Actions)

В папке `.github/workflows/` настроены два процесса:

1.  **update_flights.yml**:
    *   **Расписание:** Каждые 20 минут (`*/20 * * * *`).
    *   **Действие:** Запускает `node scripts/scrape.js`, обновляет `flights.json`, пушит изменения в ветку.

2.  **update_zimnik.yml**:
    *   **Расписание:** Каждые 6 часов (`0 */6 * * *`).
    *   **Действие:** Запускает `node scripts/scrape_zimnik.js`, обновляет `zimnik.json`, пушит изменения.

---

## 📂 Структура файлов

```text
├── assets/             # Картинки (webp), иконки
├── css/                # Стили
│   ├── styles.css      # Основные стили
│   └── heli.css        # Стили вертолета и анимаций полета
├── js/                 # Логика приложения
│   ├── config.js       # Настройки (координаты, интервалы)
│   ├── main.js         # Главный контроллер, запускает все модули
│   ├── weather.js      # Загрузка и отрисовка погоды
│   ├── zimnik.js       # Чтение zimnik.json и отображение бегущей строки
│   ├── planes.js       # Чтение flights.json и отрисовка самолетов (Canvas)
│   ├── heli.js         # Логика вертолета
│   ├── animations.js   # Общие анимации (открытие коробки, дыхание кота)
│   ├── mouse.js        # Логика мыши с пропеллером
│   └── ...             # Фейерверки, гирлянды
├── scripts/            # Node.js скрейперы (запускаются на сервере/локально)
│   ├── scrape.js       # Парсер авиарейсов
│   └── scrape_zimnik.js # Парсер зимников
├── flights.json        # Данные о рейсах (генерируется авто)
├── zimnik.json         # Данные о дорогах (генерируется авто)
└── index.html          # Главная страница
```

---

## ⚙️ Настройка (Configuration)

Основные параметры находятся в `js/config.js`:

```javascript
const CONFIG = {
    lat: 66.8333,      // Широта (Яр-Сале)
    lon: 70.8333,      // Долгота
    updateInterval: 600000, // Частота обновления погоды (10 мин)
    testMode: false    // Режим отладки (для проверки верстки без API)
};
```

### Важные технические моменты:
1.  **Proxy:** Скрипт `scripts/scrape_zimnik.js` жестко привязан к конкретному прокси-серверу (`62.233.44.186`). Если парсинг зимников перестанет работать, первым делом проверьте доступность этого прокси или обновите учетные данные в файле скрипта.
2.  **DDoS-Guard:** Сайт аэропорта имеет защиту. В скрипте `scrape.js` стоят специальные задержки (`waitForTimeout`), чтобы эмулировать поведение реального пользователя. Не уменьшайте эти таймауты.
